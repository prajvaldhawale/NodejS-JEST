apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-js-app
  labels:
    app: node-js-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: node-js-app
  template:
    metadata:
      labels:
        app: node-js-app
    spec:
      containers:
      - name: node-js-app
        image: prajvaldhawale380/car-booking-app:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
---
apiVersion: v1
kind: Service
metadata:
  name: node-js-app-service
spec:
  selector:
    app: node-js-app-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
